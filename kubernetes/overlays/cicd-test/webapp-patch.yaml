apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecom-webapp
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: ecom-webapp
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: DB_HOST